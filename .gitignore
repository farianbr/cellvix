# dependencies
node_modules/
client/node_modules/

# build output
dist/
.vite/

# secrets — .env holds live Atlas credentials and the JWT secret.
# Only .env.example is tracked.
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# test / audit artefacts
coverage/
test-results/
playwright-report/

# Generated screenshots only — the planning docs in docs/ are tracked.
docs/screenshots

# scratch
cellvix-project-brief.md
CLAUDE.md
PROGRESS.md
PROJECT_INSTRUCTIONS.md
scripts

# OS
.DS_Store
Thumbs.db

# Mail outbox — every message the mailer could not send (no SMTP_URL configured).
server/.mail/

# cPanel upload bundle — generated by deploy/bundle.mjs
deploy/out/
